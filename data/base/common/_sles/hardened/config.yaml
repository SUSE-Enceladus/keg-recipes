config:
  files:
    sles_hardened_variant_id:
      - path: /etc/os-release
        append: True
        content: VARIANT_ID="sles-hardened"
setup:
  scripts:
    hardened_config:
      - pcs-hardening-workarounds
      - pcs-hardening
