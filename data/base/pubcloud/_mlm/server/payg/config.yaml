config:
  scripts:
    base_pubcloud_mlm_payg_zypp_max_connections:
      - zypp-limit-concurrent-downloads
  services:
    base_pubcloud_mlm_server_payg:
      - csp-billing-adapter
      - guestregister
