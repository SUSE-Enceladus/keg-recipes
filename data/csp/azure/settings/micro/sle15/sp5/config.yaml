config:
  services:
    azure-afterburn-checkin: Null
    azure-micro-init:
      - cloud-config
      - cloud-final
      - cloud-init
      - cloud-init-local
      - cloud-init-main
      - cloud-init-network
      - waagent
