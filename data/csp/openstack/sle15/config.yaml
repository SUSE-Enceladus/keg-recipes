config:
  scripts:
    openstack-config:
      - ssh-disable-challenge-response-auth
      - ssh-disable-password-login
  services:
    vm-services:
      - haveged
    openstack-cloud-init:
      - cloud-init-local
      - cloud-init
      - cloud-config
      - cloud-final
