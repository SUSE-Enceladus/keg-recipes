config:
  scripts:
    openstack-config:
      - ssh-disable-challenge-response-auth
      - ssh-disable-password-login
  services:
    openstack-cloud-init:
      - cloud-init-local
      - cloud-init
      - cloud-config
      - cloud-final
      - cloud-init-main
      - cloud-init-network
