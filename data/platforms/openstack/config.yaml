config:
  scripts:
    platforms_openstack_config:
      - ssh-disable-challenge-response-auth
      - ssh-disable-password-login
  services:
    plaforms_openstack_cloud_init:
      - cloud-init-local
      - cloud-init
      - cloud-config
      - cloud-final
      - cloud-init-main
      - cloud-init-network
